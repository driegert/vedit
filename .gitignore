# Python-generated files
__pycache__/
*.py[oc]
build/
dist/
wheels/
*.egg-info

# Virtual environments
.venv

# Rendered video and media scratch. Use `git add -f` for a deliberate fixture.
*.mp4
*.mkv
*.mov
*.webm
*.m4a
*.wav
*.v3

# Staging files, left behind only by an interrupted render
.vedit-*

# Editor and OS cruft
.DS_Store
*.sw?

# Real screencast frames for the OCR fixture tests: extracted from a private video by
# tests/fixtures/extract.sh, never committed (see tests/fixtures/README.md)
tests/fixtures/frames/
